{
  "name": "bank-server",
  "version": "1.0.0",
  "description": "",
  "main": "json-server --watch db.json",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "hotel": "^0.8.6",
    "json-server": "^0.12.2"
  }
}
